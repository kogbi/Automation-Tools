<Window x:Class="WormholeAutomationUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:WormholeAutomationUI.Models"
        xmlns:conv="clr-namespace:WormholeAutomationUI.Converters"
        mc:Ignorable="d"
        Title="{DynamicResource AppTitle}" Height="720" Width="1100" MinHeight="600" MinWidth="1000">
    <Window.Resources>
        <conv:EnumEqualsToVisibilityConverter x:Key="EnumEqualsToVisibility" />
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Key="V" Modifiers="Control" Command="{Binding PasteTemplateCommand}" />
    </Window.InputBindings>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="12" CornerRadius="8" Background="#F3F5F7">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <TextBlock Text="{DynamicResource TargetWindowTitle}" FontWeight="SemiBold" />
                    <DockPanel>
                        <TextBox Text="{Binding WindowTitle, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" />
                        <Button Content="{DynamicResource PickWindow}" Width="90" Margin="8,0,0,0" Command="{Binding PickWindowCommand}" />
                    </DockPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,16,0">
                    <TextBlock Text="{DynamicResource MatchConfidence}" FontWeight="SemiBold" />
                    <TextBox Text="{Binding MatchConfidence, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,16,0">
                    <TextBlock Text="{DynamicResource PinHost}" FontWeight="SemiBold" />
                    <TextBox Text="{Binding PinHost, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Grid.Column="3">
                    <TextBlock Text="{DynamicResource PinPort}" FontWeight="SemiBold" />
                    <TextBox Text="{Binding PinPort, UpdateSourceTrigger=PropertyChanged}" />
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0" HorizontalAlignment="Right">
                        <CheckBox Content="{DynamicResource LoopEnabled}" IsChecked="{Binding LoopEnabled}" Margin="0,0,12,0" />
                        <Button Content="{DynamicResource LanguageToggle}" Width="64" Click="OnToggleLanguage" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,16,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Padding="12" CornerRadius="8" Background="#FFFFFF" BorderBrush="#DDDDDD" BorderThickness="1">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="{DynamicResource Steps}" FontWeight="Bold" Margin="0,0,0,8" />
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0" HorizontalAlignment="Left">
                        <Button Content="{DynamicResource Add}" Width="64" Margin="0,0,8,0" Command="{Binding AddStepCommand}" />
                        <Button Content="{DynamicResource Remove}" Width="72" Margin="0,0,8,0" Command="{Binding RemoveStepCommand}" />
                        <Button Content="{DynamicResource Up}" Width="52" Margin="0,0,8,0" Command="{Binding MoveUpCommand}" />
                        <Button Content="{DynamicResource Down}" Width="60" Command="{Binding MoveDownCommand}" />
                    </StackPanel>
                    <ListBox ItemsSource="{Binding Steps}" SelectedItem="{Binding SelectedStep}" DisplayMemberPath="Name" />
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Padding="12" CornerRadius="8" Background="#FFFFFF" BorderBrush="#DDDDDD" BorderThickness="1" Margin="16,0,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel DataContext="{Binding SelectedStep}">
                        <TextBlock Text="{DynamicResource StepEditor}" FontWeight="Bold" Margin="0,0,0,8" />

                        <StackPanel Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource Name}" FontWeight="SemiBold" />
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel Margin="0,0,0,8">
                            <TextBlock Text="{DynamicResource Action}" FontWeight="SemiBold" />
                            <ComboBox ItemsSource="{Binding DataContext.StepActions, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding Action, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel Margin="0,0,0,8">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Action}" Value="{x:Static models:StepAction.ClickImage}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="{DynamicResource TemplateFile}" FontWeight="SemiBold" />
                            <DockPanel>
                                <TextBox Text="{Binding TemplateFile, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" />
                                <Button Content="{DynamicResource Browse}" Width="72" Margin="8,0,0,0" Command="{Binding DataContext.PickTemplateCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                                <Button Content="{DynamicResource Paste}" Width="72" Margin="8,0,0,0" Command="{Binding DataContext.PasteTemplateCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                                <Button Content="{DynamicResource Clear}" Width="72" Margin="8,0,0,0" Command="{Binding DataContext.ClearTemplateCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                            </DockPanel>
                        </StackPanel>

                        <Border BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="6" Padding="8" Background="#FAFAFA" AllowDrop="True" Drop="OnTemplateDrop">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Action}" Value="{x:Static models:StepAction.ClickImage}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <Image Height="180" Stretch="Uniform" Source="{Binding TemplatePreview}" />
                                <TextBlock Text="{DynamicResource TemplateHint}" Foreground="#666666" Margin="0,8,0,0" />
                            </StackPanel>
                        </Border>

                        <StackPanel Margin="0,0,0,8">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Action}" Value="{x:Static models:StepAction.WaitImage}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="{DynamicResource TemplateFile}" FontWeight="SemiBold" />
                            <DockPanel>
                                <TextBox Text="{Binding TemplateFile, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" />
                                <Button Content="{DynamicResource Browse}" Width="72" Margin="8,0,0,0" Command="{Binding DataContext.PickTemplateCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                                <Button Content="{DynamicResource Paste}" Width="72" Margin="8,0,0,0" Command="{Binding DataContext.PasteTemplateCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                                <Button Content="{DynamicResource Clear}" Width="72" Margin="8,0,0,0" Command="{Binding DataContext.ClearTemplateCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                            </DockPanel>
                        </StackPanel>

                        <Border BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="6" Padding="8" Background="#FAFAFA" AllowDrop="True" Drop="OnTemplateDrop">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Action}" Value="{x:Static models:StepAction.WaitImage}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <Image Height="180" Stretch="Uniform" Source="{Binding TemplatePreview}" />
                                <TextBlock Text="{DynamicResource TemplateHint}" Foreground="#666666" Margin="0,8,0,0" />
                            </StackPanel>
                        </Border>

                        <StackPanel Margin="0,0,0,8">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Action}" Value="{x:Static models:StepAction.WaitImage}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="{DynamicResource Timeout}" FontWeight="SemiBold" />
                            <TextBox Text="{Binding TimeoutSec, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Action}" Value="{x:Static models:StepAction.SwipeWindow}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="{DynamicResource Swipe}" FontWeight="SemiBold" Margin="0,8,0,4" />
                            <WrapPanel>
                                <StackPanel Margin="0,0,16,8">
                                    <TextBlock Text="{DynamicResource StartX}" />
                                    <TextBox Width="80" Text="{Binding StartX, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <StackPanel Margin="0,0,16,8">
                                    <TextBlock Text="{DynamicResource StartY}" />
                                    <TextBox Width="80" Text="{Binding StartY, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <StackPanel Margin="0,0,16,8">
                                    <TextBlock Text="{DynamicResource EndX}" />
                                    <TextBox Width="80" Text="{Binding EndX, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <StackPanel Margin="0,0,16,8">
                                    <TextBlock Text="{DynamicResource EndY}" />
                                    <TextBox Width="80" Text="{Binding EndY, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                            </WrapPanel>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,8">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Action}" Value="{x:Static models:StepAction.SendSignal}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Text="{DynamicResource TextMessage}" FontWeight="SemiBold" />
                            <TextBox Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel Margin="0,8,0,8">
                            <CheckBox Content="{DynamicResource ValidateStep}" IsChecked="{Binding UseValidation}" />
                        </StackPanel>

                        <StackPanel Margin="0,0,0,8" Visibility="{Binding UseValidation, Converter={StaticResource BoolToVisibility}, TargetNullValue=Collapsed, FallbackValue=Collapsed}">
                            <TextBlock Text="{DynamicResource FailAction}" FontWeight="SemiBold" />
                            <ComboBox ItemsSource="{Binding DataContext.FailActions, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding FailAction}" />
                        </StackPanel>

                        <StackPanel Margin="0,0,0,8" Visibility="{Binding FailAction, Converter={StaticResource EnumEqualsToVisibility}, ConverterParameter={x:Static models:FailAction.JumpToStep}, TargetNullValue=Collapsed, FallbackValue=Collapsed}">
                            <TextBlock Text="{DynamicResource FailTarget}" FontWeight="SemiBold" />
                            <TextBox Text="{Binding FailTargetStepIndex, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel Margin="0,0,0,8" Visibility="{Binding FailAction, Converter={StaticResource EnumEqualsToVisibility}, ConverterParameter={x:Static models:FailAction.SendSignal}, TargetNullValue=Collapsed, FallbackValue=Collapsed}">
                            <TextBlock Text="{DynamicResource FailMessage}" FontWeight="SemiBold" />
                            <TextBox Text="{Binding FailMessage, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel Margin="0,8,0,0">
                            <TextBlock Text="{DynamicResource TemplatesFolder}" FontWeight="SemiBold" />
                            <DockPanel>
                                <TextBox Text="{Binding DataContext.TemplatesFolder, RelativeSource={RelativeSource AncestorType=Window}, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Content="{DynamicResource Browse}" Width="72" Margin="8,0,0,0" Command="{Binding DataContext.PickTemplatesFolderCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
                            </DockPanel>
                        </StackPanel>

                        <StackPanel Margin="0,8,0,0">
                            <TextBlock Text="{DynamicResource DoneMessage}" FontWeight="SemiBold" />
                            <TextBox Text="{Binding DataContext.DoneMessage, RelativeSource={RelativeSource AncestorType=Window}, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <Border Grid.Row="2" Padding="12" CornerRadius="8" Background="#F3F5F7">
            <DockPanel>
                <TextBox Text="{Binding Status}" IsReadOnly="True" BorderThickness="0" Background="Transparent"
                         VerticalAlignment="Center" TextWrapping="Wrap" />
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{DynamicResource FlowLoad}" Width="92" Margin="0,0,8,0" Command="{Binding LoadFlowCommand}" />
                    <Button Content="{DynamicResource FlowSave}" Width="92" Margin="0,0,8,0" Command="{Binding SaveFlowCommand}" />
                    <Button Content="{DynamicResource Run}" Width="72" Margin="0,0,8,0" Command="{Binding RunCommand}" />
                    <Button Content="{DynamicResource Stop}" Width="72" Command="{Binding StopCommand}" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
